Busca por código:
Foi utilizada busca binária.
Complexidade O(log n).

Busca por nome:
Foi utilizada busca linear.
Complexidade O(n).

Justificativa:
A busca por código é exata e utiliza vetor ordenado.
A busca por nome necessita percorrer todos os produtos,
pois busca correspondências parciais.